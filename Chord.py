scaleTypes: {
        'major': [[

class Scale:
    def __init__(self, intervals: str, quality: str):
        self.root = root
        self.quality = quality

class Chord:
    def __init__(self, root: str, quality: str):
        self.root = root
        self.quality = quality

chordTypes = {
        'major': ['root', 'M3', 'P5']
        'minor': ['root', 'm3', 'P5']
}
